using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;
using UnityEngine.SceneManagement;

public class UIButtonManager : MonoBehaviour
{
    public PlayerController playerController;

    // change scene based on the build index
    public void ChangeScene(){

    }

    // get a UI reference to a menu object
    public void ChangeToMenu(){

    }

    // Load Player Profile to JSON from its manager
    public void SaveProfile(){
        // playerController.SavePlayerProfile();
    }

    // Load Player Profile from JSON from its manager
    public void LoadProfile(){
        // playerController.LoadProfile();
    }
}
